import { FilterRowPipe } from './filter-row.pipe';

describe('FilterRowPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterRowPipe();
    expect(pipe).toBeTruthy();
  });
});
